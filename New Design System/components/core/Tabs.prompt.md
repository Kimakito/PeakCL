Navigation par onglets.

```jsx
<Tabs items={[{value:'web',label:'Site web'},{value:'rs',label:'Réseaux'}]} value={tab} onChange={setTab} />
```

variant="underline" pour la version soulignée dégradé bleu.
