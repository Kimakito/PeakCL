Bouton rond à icône seule (fermer, éditer, réseaux sociaux).

```jsx
<IconButton aria-label="fermer" icon={<X/>} />
<IconButton aria-label="envoyer" solid icon={<Send/>} />
```

solid = fond bleu plein. Toujours fournir aria-label.
