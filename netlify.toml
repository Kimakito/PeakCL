[build]
  command = "npm run build"
  publish = "dist/client"

# TanStack Start on Netlify runs via a serverless function.
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200

